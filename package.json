{
  "name": "twitter Real Verified",
  "version": "2.0.0",
  "description": "Identifies legacy verified users against users with Twitter Blue subscription",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:chrome": "node --experimental-modules esbuild.config.mjs -e \"import { buildChrome } from './esbuild.config.mjs'; buildChrome();\"",
    "build:firefox": "node esbuild.config.mjs chrome",
    "watch": "node esbuild.config.mjs watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "chokidar": "^3.5.3",
    "esbuild": "^0.17.15",
    "esbuild-plugin-svgr": "^1.1.0",
    "fs-extra": "^11.1.1",
    "standard": "^17.0.0"
  },
  "standard": {
    "globals": [
      "chrome",
      "browser",
      "localStorage",
      "node"
    ]
  }
}
